spring:
  application:
    name: catalog-service

server:
  port:
    8082

catalog-path:
  /csv/jcpenney_com-ecommerce_sample.csv

eureka:
  client:
    service-url:
      defaultZone: ${EUREKA_URI:http://localhost:8761/eureka}
  instance:
    prefer-ip-address: true
    hostname: localhost
